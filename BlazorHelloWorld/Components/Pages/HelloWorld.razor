@page "/helloworld"
@rendermode InteractiveServer

<PageTitle>Hello World</PageTitle>

<div class="hello-container">
    <h1>🎉 Hello World em Blazor!</h1>
    
    <div class="card">
        <p>Bem-vindo ao seu primeiro componente Blazor!</p>
        
        <div class="interactive-section">
            <label>Digite seu nome:</label>
            <input @bind="nome" @bind:event="oninput" placeholder="Seu nome aqui..." />
            
            @if (!string.IsNullOrWhiteSpace(nome))
            {
                <div class="greeting">
                    <h2>Olá, @nome! 👋</h2>
                    <p>Você clicou <strong>@contador</strong> vez(es)</p>
                    <button class="btn btn-primary" @onclick="IncrementarContador">
                        Clique Aqui!
                    </button>
                </div>
            }
        </div>
    </div>
</div>

@code {
    private string nome = "";
    private int contador = 0;

    private void IncrementarContador()
    {
        contador++;
    }
}

<style>
    .hello-container {
        text-align: center;
        padding: 2rem;
        max-width: 800px;
        margin: 0 auto;
    }

    .card {
        background: white;
        border-radius: 8px;
        padding: 2rem;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        margin-top: 2rem;
    }

    .interactive-section {
        margin-top: 2rem;
    }

    .interactive-section input {
        width: 100%;
        max-width: 400px;
        padding: 0.75rem;
        font-size: 1rem;
        border: 2px solid #ddd;
        border-radius: 4px;
        margin: 1rem 0;
    }

    .greeting {
        margin-top: 1.5rem;
        padding: 1.5rem;
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        border-radius: 8px;
    }

    .btn {
        padding: 0.75rem 2rem;
        font-size: 1rem;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        margin-top: 1rem;
    }

    .btn-primary {
        background-color: #4CAF50;
        color: white;
        transition: background-color 0.3s;
    }

    .btn-primary:hover {
        background-color: #45a049;
    }
</style>
